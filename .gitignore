# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/

# uv
.python-version

# Environment variables / secrets
.env
.env.*
!.env.example

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# Type checking / linting
.pyright/
.ruff_cache/
.mypy_cache/

# Logs (rotated loguru files, local only)
*.log
logs/

# MLflow (local runs, if used ad hoc)
mlruns/

# Drift analysis working data (downloaded reference, Postgres exports) —
# derived/reproducible, not committed. Fixtures under scripts/k6/fixtures/
# are intentionally excluded from this rule (committed).
data/

# Drift report output — regenerated on demand (CI's scripts/generate_drift_report.py
# for /reports/, `make generate-drift-report` or re-running the notebook for
# notebooks/reports/), not committed.
/reports/
notebooks/reports/

# Docs build
site/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Working materials (project brief, plan, kaggle scripts) — kept locally, not versioned
references/
